# React useState Hook Guide

`useState` is a built-in React Hook that allows you to add state (data that changes over time) to functional components.

---

## 1. Import useState

To use it, first import `useState` from React:

```jsx
import { useState } from 'react';